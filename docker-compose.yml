services:
  frontend:
    image: ghcr.io/patriot0911/xelar-front:latest
    container_name: xelar-frontend
    restart: unless-stopped
    ports:
      - "127.0.0.1:4000:4000"
    env_file:
      - .env